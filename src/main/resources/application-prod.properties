## Production profile properties
# In production, we want Flyway migrations to run automatically. Set this file active via
# SPRING_PROFILES_ACTIVE=prod or --spring.profiles.active=prod
spring.flyway.enabled=true

# Production logging directory should be set via environment variable APP_LOG_PATH
# e.g., APP_LOG_PATH=/var/log/erp
