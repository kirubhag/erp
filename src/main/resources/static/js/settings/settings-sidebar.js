// Settings Sidebar JavaScript

// Initialize sidebar sections as expanded by default
if (typeof angular !== 'undefined') {
  angular.element(document).ready(function() {
    // All sections expanded by default - no action needed
  });
}
